FROM node:alpine
# RUN npm install -g sniper
# install mongodb too, and connect to sniper
# ENTRYPOINT [ "/usr/local/bin/sniper" ]
# Change the entry point: it should be chrome browser 

# If available, add the localhost url, too.